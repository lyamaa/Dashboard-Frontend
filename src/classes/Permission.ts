import { idText } from "typescript"
export class Permission{
     id : number;
     name: string;

     constructor(id:number=0, name:string=""){
          this.id = id
          this.name = name

     }
} 